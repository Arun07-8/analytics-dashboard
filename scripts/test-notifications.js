import { initializeApp, cert } from "firebase-admin/app";
import { getFirestore } from "firebase-admin/firestore";

// Find firebase admin credentials? 
// Maybe I can just use the frontend's firebase config in client SDK?
